# a simple for loop to calculate total from a list
# e.g. if we need to total expense over a couple of months 
expense=[1000,5000,2350,5500,3400,4500]
total=0
for expX in expense:
    total = total + expX
print(total)